class Category {
    name: string
    grade: number
    weight: number
    
    constructor(name: string, grade: number, weight: number) {
        this.name = name
        this.grade = grade
        this.weight = weight
    }
}

export default Category